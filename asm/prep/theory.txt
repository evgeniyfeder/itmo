1) Регистры общего назначения, сегментные, FPU, MMX, SSE, арифметические и логические команды (сложение, xor, сдвиги...), управляющие структуры (if, циклы, switch).

64 битный мир
добавили r*
добавили r8-r15, младшие r*w r*b

FPU
r0-r7(нельзя явно обратиться) и st0-st7. Они 80 битные
все общение через память
есть регистры состояния

MMX
Появились фактически новые. Мантиссы FPU хранятся
при использовании все регистры становтся заполненными. Поэтому операция с FPU после MMX будет множить NaN.
Это интовое расшерения.
Как интепретивровать все зависит от команды. В суффикс указываем 

SSE - новое модное Intel
xmm0-xmm7 это независимые регистры.
SSE - 4 флота
SSE2 - 2 дабла + или набор целых чисел
SSE3 и SSE4 - не меняется
AWX - ymm регистры - 256 битные регистры - то
...

спец регистры
rip flags eflags cr3

сегментные
16 бит cs ds es ss fs gs

умножение и деление не работают с константами
при сдвиге оставшийся вытесняется в carry flag

2) Команды передачи управления (переходы, вызов функций, возврат), стандартные конвенции вызова функций (cdecl, stdcall, pascal, fastcall, thiscall, fastcall64, unix64).
явно или не явно ip
jmp call
retn retf


mmx - целые
sse - 4 флота
SSE2 - два добла. Суффик d

Реальный режим i8086 16 битные регистры. Но при этом он умел адресоваться по сегментам 