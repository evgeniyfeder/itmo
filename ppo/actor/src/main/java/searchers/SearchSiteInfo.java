package searchers;

public class SearchSiteInfo {
    public String host;
    public String desc;
    public int port;

    public SearchSiteInfo(String host, String desc, int port) {
        this.host = host;
        this.desc = desc;
        this.port = port;
    }
}
